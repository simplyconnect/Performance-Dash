/* =========================================================
   Simply Connect – runtime config
   Paste your deployed Apps Script Web App URL below to make
   the dashboard read live from Google Sheets. Leave it as
   null to keep using the bundled sample data.

   Where to get the URL: apps-script/README.md, step 4.
   ========================================================= */
window.SC_CONFIG = {
  feedUrl: null, // e.g. "https://script.google.com/macros/s/AKfycb.../exec"
};
